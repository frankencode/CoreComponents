/*
 * Copyright (C) 2018 Frank Mertens.
 *
 * Distribution and use is allowed under the terms of the zlib license
 * (see cc/LICENSE-zlib).
 *
 */

#pragma once

#include <cc/ui/View>

namespace cc {
namespace ui {

class TextLine;
class HLine;

class TextField: public View
{
    friend class Object;

public:
    static TextField *create(View *parent, String labelText);

    Property<String> labelText;
    Property<String> inputText;
    Property<String> placeholderText;
    Property<String> helpText;
    Property<String> statusText;
    Property<String> errorText;

private:
    TextField(View *parent, String nameText);

    Property<bool> focus;
    Property<bool> pressed;

    bool onPointerPressed(const PointerEvent *event);
    bool onPointerReleased(const PointerEvent *event);

    TextLine *labelTextLine_;
    TextLine *inputTextLine_;
    TextLine *placeholderTextLine_;
    HLine *inputLine_;
    TextLine *helpTextLine_;
    TextLine *statusTextLine_;
    TextLine *errorTextLine_;
};

}} // namespace cc::ui
