/*
 * Copyright (C) 2018 Frank Mertens.
 *
 * Distribution and use is allowed under the terms of the zlib license
 * (see cc/LICENSE-zlib).
 *
 */

#pragma once

#include <cc/ui/View>
#include <cc/ui/Font>

namespace cc {
namespace ui {

class TextRun;

class Label: public View
{
    friend class Object;

public:
    static Label *create(View *parent, const String &text = String{}, const Font &font = Font{});

    Property<String> text;
    Property<Font> font;
    Property<Color> ink;
    Property<Size> margin;

protected:
    Property< Ref<const TextRun> > textRun;

    Label(View *parent, const String &text, const Font &font_ = Font{});
    ~Label();

    void paint() override;
};

}} // namespace cc::ui
