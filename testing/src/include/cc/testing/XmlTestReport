/*
 * Copyright (C) 2007-2017 Frank Mertens.
 *
 * Distribution and use is allowed under the terms of the zlib license
 * (see cc/LICENSE-zlib).
 *
 */

#pragma once

#include <cc/testing/TestReport>

namespace cc {
namespace testing {

class XmlTestReport: public TestReport
{
public:
    inline static Ref<XmlTestReport> create(Stream *stream) { return new XmlTestReport(stream); }

    virtual bool captureOutput() const;

    virtual void beginTestSuite(TestSuite *testSuite) override;
    virtual void beginTestCase(TestCase *testCase) override;
    virtual void verify(TestCase *testCase, bool condition, String message, String codePath, int codeLine) override;
    virtual void error(TestCase *testCase, String type, String message) override;
    virtual void endTestCase(TestCase *testCase, String outText = "", String errText = "") override;
    virtual void skipTestCase(TestCase *testCase) override;
    virtual void endTestSuite(TestSuite *testSuite) override;

protected:
    XmlTestReport(Stream *stream);

private:
    static String xmlEscape(String text);

    Ref<Stream> stream_;
};

}} // namespace cc::testing
