Installation Instructions
=========================

You need the lastest version of cmake (>2.8.0).

$tar xzvf libPONA-x.y.z.tar.gz
$mkdir libPONA-x.y.z-debug
$cd libPONA-x.y.z-debug
$ccmake ../libPONA-x.y.z
... configure interactively, then generate makefiles ...
$make
$sudo make install
