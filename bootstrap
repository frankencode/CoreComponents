#!/bin/sh

if [ $# -ne 0 ]; then
	echo "Usage: path/to/fkit/bootstrap"
	echo
	echo "Bootstraps fkit's build system (fmake)."
	exit 1
fi

if [ -f $PWD/Recipe ]; then
	echo "I'd like to be called out-of-source."
	exit 1
fi

$(dirname $0)/make/bootstrap $(dirname $0)
