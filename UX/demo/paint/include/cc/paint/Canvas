#pragma once

#include <cc/View>

namespace cc::paint {

/** The drawing canvas
  */
class Canvas final: public View
{
public:
    /** Create a new canvas of initial size (\a width, \a height)
      */
    Canvas(double width, double height);

private:
    struct State;
};

} // namespace cc::paint
